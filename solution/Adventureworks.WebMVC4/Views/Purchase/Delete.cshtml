@model Adventureworks.Domain5.VendorProposal

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>VendorProposal</legend>

    <div class="display-label">VendorId</div>
    <div class="display-field">@Model.VendorId</div>

    <div class="display-label">Date</div>
    <div class="display-field">@String.Format("{0:g}", Model.Date)</div>

    <div class="display-label">Value</div>
    <div class="display-field">@String.Format("{0:F}", Model.Value)</div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}


